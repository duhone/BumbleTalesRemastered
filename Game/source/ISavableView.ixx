/*
 *  ISavableView.h
 *  Bumble Tales
 *
 *  Created by Robert Shoemate on 6/27/09.
 *  Copyright 2009 Conjured Realms LLC. All rights reserved.
 *
 */
export module CR.Game.ISavableView;

import std;
import std.compat;

export namespace CR::Game {
	/*
	* This was all commented out in last version of HPT version of this game. I don't recall details.
	* so just keeping it here commented out for now. just delete once im sure I don't need it.
	#include "SaveInfo.h"

	// Implement this on View that support saving and loading. The view must know the type of save info it is
	recieving

	class ISavableView
	{
	public:
	    virtual ~ISavableView() {}

	    void LoadFromSaveInfo(SaveInfo *saveInfo);
	    void WriteToSaveInfo(SaveInfo *saveInfo);

	protected:
	    ISavableView() {};
	}*/
}    // namespace CR::Game

module :private;
